# Vercel / CI: skip optional native deps (canvas via pdfjs-dist → @mapbox/node-pre-gyp).
# That chain pulls deprecated npmlog/rimraf/tar/glob and is not needed for next build.
omit=optional

# Prefer lockfile determinism
engine-strict=false
