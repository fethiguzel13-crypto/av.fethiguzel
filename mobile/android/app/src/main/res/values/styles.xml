<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Taban tema. colorPrimary / colorPrimaryDark flavor'a göre
         build.gradle içinde resValue ile üretilir; colorAccent colors.xml'de. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>

    <!-- Açılış ekranı. androidx.core.splashscreen API 31 öncesinde de sistem
         splash davranışını taklit eder; eski `android:background` yaklaşımı
         API 31+ cihazlarda çift splash gösteriyordu. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/colorPrimary</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
